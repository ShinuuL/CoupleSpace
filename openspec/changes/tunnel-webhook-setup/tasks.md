## Tasks

- [x] 1. Verify tunnel connectivity — tunnel `couplespace-api` está rodando com conexões ativas
- [x] 2. Set Supabase secrets: `FIREBASE_PROJECT_ID` e `FIREBASE_CLIENT_EMAIL` configurados; `FIREBASE_PRIVATE_KEY` pendente (usuário tem a chave no .env); service role key não necessária (RLS aberto)
- [ ] 3. Create database webhook on `messages` INSERT (usuário tentou via Integrations; precisa confirmar se está funcionando via POST test)
- [ ] 4. Test push notification end-to-end
- [x] 5. Update archived supabase-backend-integration tasks
- [x] 6. Update README with final webhook setup instructions
